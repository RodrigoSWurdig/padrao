cubes:
  - name: maids
    sql_table: dev.derived.vector_maid # Table

    joins:
      - name: universal_person
        relationship: many_to_one
        sql: "{CUBE}.vup_id = {universal_person}.vup_id"

    dimensions:
      - name: id
        sql: vup_id
        type: string
        primary_key: true
        public: true

      - name: maid
        sql: maid
        type: string

      - name: maid_type
        sql: maid_type
        type: string

      - name: data_source
        sql: data_source
        type: string

      - name: dataset_version
        sql: dataset_version
        type: string
