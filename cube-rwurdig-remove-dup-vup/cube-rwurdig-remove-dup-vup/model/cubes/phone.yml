cubes:
  - name: phones
    sql_table: dev.derived.vector_phone # Table

    joins:
      - name: universal_person
        relationship: many_to_one
        sql: "{CUBE}.vup_id = {universal_person}.vup_id"

    dimensions:
      - name: id
        sql: vup_id
        type: string
        primary_key: true

      - name: phone
        sql: phone
        type: string

      - name: phone_type
        sql: phone_type
        type: string

      - name: data_source
        sql: data_source
        type: string

      - name: dataset_version
        sql: dataset_version
        type: string
